# Prompt日志
## 生成访谈提纲
请基于我们这个平台的项目说明文档和需求调查问卷，生成一份访谈提纲，包括 5-8 个开放式问题
```claude
● 先读取相关文档。


● 文档内容已读清楚。现在基于 SRS 和问卷生成访谈提纲并写入文件。

  Listed 1 directory (ctrl+o to expand)


● 写入成功。生成的访谈提纲共 7 个开放式问题，结构如下：

  | # | 主题 | 设计意图 |
  | --- | --- | --- |
  | Q1 | 背景与经历 | 建立上下文，了解受访者真实参与深度 |
  | Q2 | 参与障碍 | 验证 SRS 中"新手缺乏实战路径"的核心假设，深挖问卷 Q4 |
  | Q3 | 任务选择与执行 | 还原任务大厅决策过程，验证 FR-QST-007/008 的信息设计 |
  | Q4 | 反馈与激励 | 探查问卷 Q10/Q11 背后的真实动机权重 |
  | Q5 | 游戏化设计 | 评估"冒险家公会"核心概念的接受度与风险，深挖问卷 Q12/Q13 |
  | Q6 | 维护者视角 | 验证委托人侧需求（FR-QST-001、FR-DSH-002），选问 |
  | Q7 | 整体印象与开放反馈 | 收集超出预设框架的风险点与盲区 |

  每道主问下附 1-2 个追问探针（▸），访谈者可视对话情况灵活使用。
```

## 生成需求规格说明书
请为我们的这个平台生成完整的需求规格说明书：一个类似于github的代码托管平台，额外添加“冒险家协会”等游戏化世界观，将任务包装为悬赏委托，用户塑造为冒险家。除此之外，还可以允许开源项目制作者分享出他的项目，吸引（或匹配）一些想自学做项目的新手。我们还可以通过寻找github上面有beginner-friendly或者good-first-issue标签的项目，推荐给新手。目标用户为程序员。请按 IEEE 830 标准格式输出。
```claude
Now let me create the comprehensive SRS document.
需求规格说明书已生成，按 IEEE 830 标准组织为五章：
第1章 引言 — 编写目的、项目背景、术语定义（冒险家/委托人/Quest/XP 等10个术语）、参考文献
第2章 总体描述 — 产品视角（三个子系统的关系）、用户角色表、功能概览、运行环境、约束与假设
第3章 具体需求 — 38条功能需求，每条带有唯一编号（FR-USR/GIT/QST/GRW/MAT/COM/WHK/DSH），覆盖用户系统、代码托管、任务悬赏（含完整状态机表）、成长激励（等级表+徽章表）、智能匹配、社区互动、Webhook 自动化、数据看板
第4章 非功能需求 — 性能、安全、可用性、可维护性、可移植性、可扩展性共14条
第5章 附录 — 核心数据库表设计、API 接口规划、需求追溯矩阵（需求编号→模块→优先级→开发阶段）
文档中所有表格、编号、优先级标注都已就位，可以直接用于答辩或提交。
``` 

## 生成用例图

你现在是一名资深需求分析师和 UML 建模专家，熟悉软件工程课程中的用例建模规范。请基于以下项目简报，为项目生成一份高质量的系统级用例图。

任务要求：
1. 先识别系统边界、主要参与者和核心用例。
2. 再输出一份系统级用例图的 PlantUML 源码。
3. 最后简要说明每个参与者和关键关系的建模理由。

建模约束：
1. 用例图必须遵循软件工程课程中的规范：
   - 参与者（Actor）是外部角色，不是系统内部组件
   - 用例（Use Case）必须是“对参与者有价值的服务”
   - 不要把“登录”“数据校验”“连接数据库”“调用接口”这类无独立业务价值的技术动作画成用例
   - 不要混入数据库、缓存、前端页面、接口、实现细节
2. 用例粒度要合适：
   - 一个用例应对应一个业务目标或业务事件
   - 不要把一个用例拆成 CRUD 小动作
   - 也不要把多个业务目标混成一个过大的用例
3. 关系使用要严格：
   - `<<include>>` 只用于“必须发生的共同行为”
   - `<<extend>>` 只用于“特定条件下才发生的附加行为”
   - 如果没有充分理由，不要滥用 include/extend
4. 本次建模以课程展示版 MVP 为主，不要扩展成完整商业平台
5. 优先覆盖核心业务链路，不要加入过多远期功能

输出要求：
1. 先输出：
   - 系统边界名称
   - 参与者列表
   - 核心用例列表
2. 然后输出完整 PlantUML 代码，在本地新建一个.md文件保存 
3. 然后输出“建模说明”
4. 用例总数控制在 10~16 个之间，避免过多
5. 参与者优先考虑：
   - 冒险家
   - 委托人
   - 管理员
   - GitHub（外部项目来源）
   - 代码托管底座（如 Gitea，若你认为应作为外部系统参与者）
   - 定时任务/同步任务（仅在确有必要时保留）
6. 建模重点必须覆盖：
   - 项目仓库导入/接入
   - 浏览仓库与 Issue
   - 基于 Issue 发布悬赏任务
   - 浏览与筛选任务
   - 接取任务
   - 提交任务成果 / 提交 PR
   - 审核任务成果
   - 任务状态流转
   - PR 与任务完成联动
   - 查看成长结果
   - 管理员处理异常或违规任务（课程展示版中的基础管理）

项目简报如下：
...（省略）


## 生成详细用例描述

继续生成3个详细用例描述

## 生成用户故事

你现在是一名资深需求分析师，擅长将项目简报转化为高质量、可测试的用户故事。

请基于以下项目简报，为三个角色分别生成用户故事：
- 冒险家：5 条
- 委托人：5 条
- 管理员：5 条

总计 15 条用户故事，且必须满足以下要求：

1. 每条用户故事必须符合 INVEST 准则

2. 每条用户故事必须使用标准格式：
作为[角色]，我希望[功能]，以便[价值]。

3. 每条用户故事后必须附带 3 条验收标准，且全部使用 Given-When-Then 格式。

4. 每条用户故事的 3 条验收标准中，至少 1 条必须覆盖异常路径或边界场景，例如但不限于：
- 网络中断
- 权限不足
- 数据缺失
- 任务已被他人领取
- 仓库导入失败
- 外部平台接口不可用
- PR 关联失败
- 提交内容不符合要求

5. 验收标准必须具体、清晰、可测试，禁止使用模糊表达，如：
- “方便”
- “快速”
- “及时”
- “支持”
- “友好”
如确需表达性能或时效，必须量化或条件化。

6. 不要混入以下内容：
- 技术实现细节
- 数据库设计
- API 设计
- 页面配色或具体 UI 样式
- “登录”“数据校验”“连接数据库”这类没有独立业务价值的伪故事
除非其本身在项目简报中被明确说明为独立业务目标。

7. 用户故事应优先覆盖项目的核心业务链路，并避免重复、冲突或空泛表述。

8. 如果项目简报信息不足，不要擅自发明复杂机制；请基于简报做最小合理假设，并直接体现在故事和验收标准中，不要额外解释。

输出要求：
- 仅输出 Markdown 列表
- 按角色分组输出，使用以下结构：
## 冒险家
1.
2.
3.
4.
5.

## 委托人
1.
2.
3.
4.
5.

## 管理员
1.
2.
3.
4.
5.
- 每个编号下包含：
  - 用户故事
  - 验收标准 1
  - 验收标准 2
  - 验收标准 3
- 不要输出任何解释、前言、总结、注释或额外说明

输出前请静默自检：
- 是否共 15 条故事
- 是否每条都有 3 条 Given-When-Then
- 是否每条至少 1 条异常路径
- 是否满足 INVEST
- 是否避免模糊词和实现细节

项目简报如下：
...（省略）
