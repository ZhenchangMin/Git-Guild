# Git Guild 核心类图

## 1. 说明

本文件按 P3 阶段 Mermaid 可视化规则重构类图。每张图都应能独立表达一个边界上下文的核心类、关键属性、关键方法和类之间关系。为保证 GitHub 预览清晰，整体类图仍拆分为四组。

类图按边界上下文拆分为四组：

| 分组           | 覆盖范围                                   |
| -------------- | ------------------------------------------ |
| 用户与外部支撑 | 用户、认证、外部代码托管适配               |
| 任务核心       | 仓库、Issue、任务、分类、标签、接取        |
| 提交审核与成长 | 成果提交、维护者审核、管理员审核、成长记录 |
| 推荐与通知     | 推荐策略、推荐结果、通知偏好、通知投递     |

## 2. 用户与外部支撑

```mermaid
%%{init: {"theme": "base", "themeVariables": {"primaryColor": "#ffffff", "primaryTextColor": "#111111", "primaryBorderColor": "#111111", "lineColor": "#111111", "secondaryColor": "#f7f7f7", "tertiaryColor": "#ffffff"}}}%%
classDiagram
    direction LR

    class User {
        +Long userId
        +String username
        +String email
        +UserRole role
        +String status
        +hasRole(role) Boolean
        +canManageQuest(quest) Boolean
    }

    class UserRole {
        <<enumeration>>
        BEGINNER
        MAINTAINER
        ADMIN
    }

    class AuthService {
        +register(command) User
        +login(command) TokenPair
        +changePassword(userId, newPassword) void
    }

    class CodeHostAccountBinding {
        +Long bindingId
        +Long userId
        +String hostType
        +String externalAccountId
        +String status
        +activate() void
        +revoke() void
    }

    class CodeHostAdapter {
        <<interface>>
        +importRepository(command) Repository
        +syncIssues(repositoryId) List~Issue~
        +getPullRequest(externalPrId) PullRequest
        +handleWebhook(payload) CodeHostEvent
    }

    class GitHubImportAdapter {
        +importRepository(command) Repository
        +syncIssues(repositoryId) List~Issue~
        +handleWebhook(payload) CodeHostEvent
    }

    class GiteaAdapter {
        +importRepository(command) Repository
        +syncIssues(repositoryId) List~Issue~
        +getPullRequest(externalPrId) PullRequest
        +handleWebhook(payload) CodeHostEvent
    }

    class Repository {
        +Long repositoryId
        +String name
        +String sourceUrl
        +String hostType
        +String syncStatus
        +isAvailable() Boolean
        +markSyncFailed(reason) void
    }

    User --> UserRole : owns platform role
    User "1" o-- "0..*" CodeHostAccountBinding : binds optional accounts
    AuthService ..> User : creates authenticated user
    CodeHostAdapter <|.. GitHubImportAdapter : imports external source
    CodeHostAdapter <|.. GiteaAdapter : supports internal collaboration
    GitHubImportAdapter ..> Repository : imports metadata
    GiteaAdapter ..> Repository : synchronizes runtime state
```

VISUAL EXPLANATION

- User roles anchor authentication and downstream permission checks consistently across modules.
- Account bindings remain separate from platform-owned authentication and user identity.
- Adapters isolate GitHub import concerns from Gitea collaboration operations clearly.
- Repository metadata stays independent from concrete external platform client implementations.

## 3. 任务核心

```mermaid
%%{init: {"theme": "base", "themeVariables": {"primaryColor": "#ffffff", "primaryTextColor": "#111111", "primaryBorderColor": "#111111", "lineColor": "#111111", "secondaryColor": "#f7f7f7", "tertiaryColor": "#ffffff"}}}%%
classDiagram
    direction LR

    class Repository {
        +Long repositoryId
        +String name
        +String hostType
        +String syncStatus
        +isAvailable() Boolean
    }

    class Issue {
        +Long issueId
        +Long repositoryId
        +String externalIssueId
        +String title
        +String status
        +canCreateQuest() Boolean
    }

    class PullRequest {
        +Long pullRequestId
        +Long repositoryId
        +String externalPrId
        +String status
        +belongsTo(repository) Boolean
    }

    class ProjectGuide {
        +Long guideId
        +Long repositoryId
        +String projectStructure
        +String runInstructions
        +String contributionSteps
        +getGuideContent() String
    }

    class Quest {
        +Long questId
        +Long publisherId
        +String title
        +QuestStatus status
        +Difficulty difficulty
        +Integer rewardXp
        +canBeAccepted() Boolean
        +publishForAdminReview() void
        +markPublished() void
        +close() void
    }

    class QuestStatus {
        <<enumeration>>
        DRAFT
        PENDING_ADMIN_REVIEW
        PUBLISHED
        IN_PROGRESS
        IN_REVIEW
        COMPLETED
        CLOSED
    }

    class Difficulty {
        <<enumeration>>
        A
        B
        C
        D
    }

    class QuestCategory {
        +Long categoryId
        +String name
        +String description
        +Boolean enabled
        +disable() void
    }

    class QuestTag {
        +Long tagId
        +String name
        +String color
        +Boolean enabled
        +rename(newName) void
    }

    class QuestAssignment {
        +Long assignmentId
        +Long questId
        +Long assigneeId
        +String status
        +DateTime acceptedAt
        +abandon() void
        +complete() void
    }

    class QuestFilterCriteria {
        +String keyword
        +Long categoryId
        +List~Long~ tagIds
        +String difficulty
        +String techStack
        +hasAnyFilter() Boolean
    }

    class QuestService {
        +createQuest(command) Quest
        +acceptQuest(questId, userId) QuestAssignment
        +searchQuests(criteria) Page~Quest~
        +reopenQuest(questId) Quest
    }

    class User {
        +Long userId
        +String username
        +UserRole role
    }

    Repository "1" *-- "0..*" Issue : contains issues
    Repository "1" *-- "0..*" PullRequest : contains pull requests
    Repository "1" *-- "0..1" ProjectGuide : owns guide
    Quest --> QuestStatus : state machine
    Quest --> Difficulty : difficulty grade
    Quest "*" --> "1" Repository : belongs to repository
    Quest "0..*" --> "1" Issue : originates from issue
    Quest "*" --> "1" User : published by maintainer
    Quest "*" --> "1" QuestCategory : classified by category
    Quest "*" o-- "0..*" QuestTag : marked with tags
    Quest "1" *-- "0..*" QuestAssignment : records assignments
    QuestAssignment "*" --> "1" User : accepted by user
    QuestService ..> Quest : controls lifecycle
    QuestService ..> QuestAssignment : creates assignment
    QuestService ..> QuestFilterCriteria : applies filters
```

VISUAL EXPLANATION

- Repository context supplies issues, pull requests, and project guidance.
- Quest aggregates classification, tags, state, difficulty, and assignment history.
- Filter criteria separates user search intent from recommendation decisions.
- Service methods enforce lifecycle transitions instead of frontend state changes.

## 4. 提交审核与成长

```mermaid
%%{init: {"theme": "base", "themeVariables": {"primaryColor": "#ffffff", "primaryTextColor": "#111111", "primaryBorderColor": "#111111", "lineColor": "#111111", "secondaryColor": "#f7f7f7", "tertiaryColor": "#ffffff"}}}%%
classDiagram
    direction LR

    class User {
        +Long userId
        +String username
        +UserRole role
    }

    class Quest {
        +Long questId
        +String title
        +QuestStatus status
        +Integer rewardXp
    }

    class PullRequest {
        +Long pullRequestId
        +String externalPrId
        +String status
        +belongsTo(repository) Boolean
    }

    class Submission {
        +Long submissionId
        +Long questId
        +Long submitterId
        +Long pullRequestId
        +String status
        +isLinkedToQuest(quest) Boolean
        +markInvalid(reason) void
    }

    class ReviewRecord {
        +Long reviewId
        +Long submissionId
        +Long reviewerId
        +String decision
        +String summary
        +requiresChanges() Boolean
    }

    class ReviewItem {
        +Long itemId
        +Long reviewId
        +String checkpoint
        +String comment
        +Boolean passed
        +markPassed() void
    }

    class AdminReviewRecord {
        +Long adminReviewId
        +Long questId
        +Long adminId
        +String decision
        +String reason
        +approvePublish() void
        +rejectPublish(reason) void
    }

    class ReviewService {
        +submitWork(command) Submission
        +reviewSubmission(command) ReviewRecord
        +reviewQuest(command) AdminReviewRecord
    }

    class GrowthProfile {
        +Long profileId
        +Long userId
        +Integer totalXp
        +Integer level
        +addXp(amount) void
        +canLevelUp() Boolean
    }

    class XpTransaction {
        +Long transactionId
        +Long userId
        +Long questId
        +Integer amount
        +String reason
    }

    class ContributionRecord {
        +Long recordId
        +Long userId
        +Long questId
        +Long repositoryId
        +String summary
    }

    class GrowthService {
        +handleReviewApproved(event) void
        +getProfile(userId) GrowthProfile
        +listContributions(userId) List~ContributionRecord~
    }

    Submission "*" --> "1" Quest : submitted for quest
    Submission "*" --> "1" User : created by submitter
    Submission "0..*" --> "0..1" PullRequest : references pull request
    Submission "1" *-- "0..*" ReviewRecord : owns review history
    ReviewRecord "1" *-- "0..*" ReviewItem : contains checkpoints
    AdminReviewRecord "*" --> "1" Quest : governs publication
    AdminReviewRecord "*" --> "1" User : performed by admin
    ReviewService ..> Submission : accepts work
    ReviewService ..> ReviewRecord : records maintainer review
    ReviewService ..> AdminReviewRecord : records admin decision
    GrowthProfile "1" --> "1" User : belongs to user
    XpTransaction "*" --> "1" User : rewards user
    XpTransaction "*" --> "1" Quest : rewards quest completion
    ContributionRecord "*" --> "1" User : summarizes contribution
    ContributionRecord "*" --> "1" Quest : references completed quest
    GrowthService ..> GrowthProfile : updates level
    GrowthService ..> XpTransaction : writes xp ledger
    GrowthService ..> ContributionRecord : writes contribution history
```

VISUAL EXPLANATION

- Submissions connect completed work with pull request evidence directly.
- Review records preserve decisions while review items capture granular feedback.
- Administrator reviews govern quest publication independently from submission review.
- Approved reviews trigger growth ledgers and visible contribution records.

## 5. 推荐与通知

```mermaid
%%{init: {"theme": "base", "themeVariables": {"primaryColor": "#ffffff", "primaryTextColor": "#111111", "primaryBorderColor": "#111111", "lineColor": "#111111", "secondaryColor": "#f7f7f7", "tertiaryColor": "#ffffff"}}}%%
classDiagram
    direction LR

    class User {
        +Long userId
        +String username
        +String role
    }

    class Quest {
        +Long questId
        +String title
        +String difficulty
        +Integer rewardXp
    }

    class RecommendationStrategy {
        <<interface>>
        +score(user, quest) Decimal
        +reason(user, quest) String
    }

    class TechStackMatchStrategy {
        +score(user, quest) Decimal
        +reason(user, quest) String
    }

    class GrowthStageStrategy {
        +score(user, quest) Decimal
        +reason(user, quest) String
    }

    class RecommendationResult {
        +Long resultId
        +Long userId
        +Long questId
        +Decimal score
        +String reasonText
        +isStrongMatch() Boolean
    }

    class RecommendationService {
        +recommendQuests(userId) List~RecommendationResult~
        +recommendContributors(questId) List~User~
        +explain(userId, questId) String
    }

    class Notification {
        +Long notificationId
        +Long receiverId
        +String type
        +String content
        +String status
        +markAsRead() void
    }

    class NotificationPreference {
        +Long preferenceId
        +Long userId
        +Boolean enableEmail
        +Boolean enableDigest
        +update(email, digest) void
    }

    class NotificationSender {
        <<interface>>
        +send(notification) SendResult
        +supports(type) Boolean
    }

    class InAppNotificationSender {
        +send(notification) SendResult
        +supports(type) Boolean
    }

    class EmailNotificationSender {
        +send(notification) SendResult
        +supports(type) Boolean
    }

    class NotificationService {
        +notifyQuestAccepted(assignment) void
        +notifyReviewResult(review) void
        +sendDailyDigest(userId) void
    }

    RecommendationStrategy <|.. TechStackMatchStrategy : scores technical fit
    RecommendationStrategy <|.. GrowthStageStrategy : scores growth fit
    RecommendationService ..> RecommendationStrategy : combines strategies
    RecommendationService ..> RecommendationResult : produces explainable result
    RecommendationResult "*" --> "1" User : recommended to user
    RecommendationResult "*" --> "1" Quest : recommends quest
    Notification "*" --> "1" User : delivered to receiver
    NotificationPreference "1" --> "1" User : configures channels
    NotificationSender <|.. InAppNotificationSender : persists in app
    NotificationSender <|.. EmailNotificationSender : sends email
    NotificationService ..> NotificationSender : selects channel
    NotificationService ..> Notification : creates notification
```

VISUAL EXPLANATION

- Recommendation strategies expose extensible scoring without changing service orchestration.
- Results persist explainable matches between users and available quests.
- Notification preferences configure delivery without changing notification business events.
- Sender interfaces isolate delivery mechanisms from notification workflow decisions.

## 6. 设计模式应用说明

| 设计模式        | 应用位置                                                 | 使用理由                                                         | 如果不使用的后果                                                |
| --------------- | -------------------------------------------------------- | ---------------------------------------------------------------- | --------------------------------------------------------------- |
| 适配器模式      | `CodeHostAdapter`、`GitHubImportAdapter`、`GiteaAdapter` | 屏蔽 GitHub 与 Gitea API 差异，避免业务模块直接依赖外部平台      | 任务、审核、新手引导模块会散落外部 API 调用，后续替换底座成本高 |
| 策略模式        | `RecommendationStrategy` 及其实现类                      | 推荐系统是核心能力，后续需要按技术栈、成长阶段、历史行为扩展规则 | 新增推荐规则时会频繁修改 `RecommendationService`，违反开闭原则  |
| 策略 / 端口模式 | `NotificationSender` 及其实现类                          | 站内通知和邮件通知有不同投递方式，但上层通知流程应保持一致       | 通知服务会同时处理业务规则和具体通道，单一职责不清              |
