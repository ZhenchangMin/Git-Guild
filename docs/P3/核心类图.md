# Git Guild 核心类图

## 1. 说明

本文件按 P3 阶段 Mermaid 可视化规则重构类图。每张图都应能独立表达一个边界上下文的核心类、关键属性、关键方法和类之间关系。为保证 GitHub 预览清晰，整体类图仍拆分为四组。

类图按边界上下文拆分为四组：

| 分组           | 覆盖范围                                   |
| -------------- | ------------------------------------------ |
| 用户与外部支撑 | 用户、认证、外部代码托管适配               |
| 任务核心       | 仓库、议题、任务、分类、标签、接取         |
| 提交审核与成长 | 成果提交、维护者审核、管理员审核、成长记录 |
| 推荐与通知     | 推荐策略、推荐结果、通知偏好、通知投递     |

## 2. 用户与外部支撑

```mermaid
%%{init: {"theme": "base", "themeVariables": {"primaryColor": "#ffffff", "primaryTextColor": "#111111", "primaryBorderColor": "#111111", "lineColor": "#111111", "secondaryColor": "#f7f7f7", "tertiaryColor": "#ffffff"}}}%%
classDiagram
    direction LR

    class User {
        +Long userId
        +String username
        +String email
        +UserRole role
        +String status
        +hasRole(role) Boolean
        +canManageQuest(quest) Boolean
    }

    class UserRole {
        <<enumeration>>
        BEGINNER
        MAINTAINER
        ADMIN
    }

    class AuthService {
        +register(command) User
        +login(command) TokenPair
        +changePassword(userId, newPassword) void
    }

    class CodeHostAccountBinding {
        +Long bindingId
        +Long userId
        +String hostType
        +String externalAccountId
        +String status
        +activate() void
        +revoke() void
    }

    class CodeHostAdapter {
        <<interface>>
        +importRepository(command) Repository
        +syncIssues(repositoryId) List~Issue~
        +getPullRequest(externalPrId) PullRequest
        +handleWebhook(payload) CodeHostEvent
    }

    class GitHubImportAdapter {
        +importRepository(command) Repository
        +syncIssues(repositoryId) List~Issue~
        +handleWebhook(payload) CodeHostEvent
    }

    class GiteaAdapter {
        +importRepository(command) Repository
        +syncIssues(repositoryId) List~Issue~
        +getPullRequest(externalPrId) PullRequest
        +handleWebhook(payload) CodeHostEvent
    }

    class Repository {
        +Long repositoryId
        +String name
        +String sourceUrl
        +String hostType
        +String syncStatus
        +isAvailable() Boolean
        +markSyncFailed(reason) void
    }

    User --> UserRole : 平台角色
    User "1" o-- "0..*" CodeHostAccountBinding : 绑定外部账号
    AuthService ..> User : 创建认证用户
    CodeHostAdapter <|.. GitHubImportAdapter : 导入外部来源
    CodeHostAdapter <|.. GiteaAdapter : 支撑平台内协作
    GitHubImportAdapter ..> Repository : 导入仓库元数据
    GiteaAdapter ..> Repository : 同步运行状态
```

可视化说明

- 用户角色支撑认证流程和跨模块权限判断。
- 外部账号绑定独立于平台自有身份体系。
- 适配器隔离 GitHub 导入和 Gitea 协作差异。
- 仓库元数据不依赖具体外部平台客户端。

## 3. 任务核心

```mermaid
%%{init: {"theme": "base", "themeVariables": {"primaryColor": "#ffffff", "primaryTextColor": "#111111", "primaryBorderColor": "#111111", "lineColor": "#111111", "secondaryColor": "#f7f7f7", "tertiaryColor": "#ffffff"}}}%%
classDiagram
    direction LR

    class Repository {
        +Long repositoryId
        +String name
        +String hostType
        +String syncStatus
        +isAvailable() Boolean
    }

    class Issue {
        +Long issueId
        +Long repositoryId
        +String externalIssueId
        +String title
        +String status
        +canCreateQuest() Boolean
    }

    class PullRequest {
        +Long pullRequestId
        +Long repositoryId
        +String externalPrId
        +String status
        +belongsTo(repository) Boolean
    }

    class ProjectGuide {
        +Long guideId
        +Long repositoryId
        +String projectStructure
        +String runInstructions
        +String contributionSteps
        +getGuideContent() String
    }

    class Quest {
        +Long questId
        +Long publisherId
        +String title
        +QuestStatus status
        +Difficulty difficulty
        +Integer rewardXp
        +canBeAccepted() Boolean
        +publishForAdminReview() void
        +markPublished() void
        +close() void
    }

    class QuestStatus {
        <<enumeration>>
        DRAFT
        PENDING_ADMIN_REVIEW
        PUBLISHED
        IN_PROGRESS
        IN_REVIEW
        COMPLETED
        CLOSED
    }

    class Difficulty {
        <<enumeration>>
        A
        B
        C
        D
    }

    class QuestCategory {
        +Long categoryId
        +String name
        +String description
        +Boolean enabled
        +disable() void
    }

    class QuestTag {
        +Long tagId
        +String name
        +String color
        +Boolean enabled
        +rename(newName) void
    }

    class QuestAssignment {
        +Long assignmentId
        +Long questId
        +Long assigneeId
        +String status
        +DateTime acceptedAt
        +abandon() void
        +complete() void
    }

    class QuestFilterCriteria {
        +String keyword
        +Long categoryId
        +List~Long~ tagIds
        +String difficulty
        +String techStack
        +hasAnyFilter() Boolean
    }

    class QuestService {
        +createQuest(command) Quest
        +acceptQuest(questId, userId) QuestAssignment
        +searchQuests(criteria) Page~Quest~
        +reopenQuest(questId) Quest
    }

    class User {
        +Long userId
        +String username
        +UserRole role
    }

    Repository "1" *-- "0..*" Issue : 包含议题
    Repository "1" *-- "0..*" PullRequest : 包含合并请求
    Repository "1" *-- "0..1" ProjectGuide : 拥有项目指引
    Quest --> QuestStatus : 状态机
    Quest --> Difficulty : 难度等级
    Quest "*" --> "1" Repository : 属于仓库
    Quest "0..*" --> "1" Issue : 来源议题
    Quest "*" --> "1" User : 由维护者发布
    Quest "*" --> "1" QuestCategory : 按分类归属
    Quest "*" o-- "0..*" QuestTag : 标记标签
    Quest "1" *-- "0..*" QuestAssignment : 记录接取
    QuestAssignment "*" --> "1" User : 由用户接取
    QuestService ..> Quest : 控制生命周期
    QuestService ..> QuestAssignment : 创建接取记录
    QuestService ..> QuestFilterCriteria : 应用筛选条件
```

可视化说明

- 仓库上下文提供议题、合并请求和项目指引。
- 任务聚合分类、标签、状态、难度和接取历史。
- 筛选条件表达用户主动查找任务的意图。
- 服务方法负责约束任务生命周期状态流转。

## 4. 提交审核与成长

```mermaid
%%{init: {"theme": "base", "themeVariables": {"primaryColor": "#ffffff", "primaryTextColor": "#111111", "primaryBorderColor": "#111111", "lineColor": "#111111", "secondaryColor": "#f7f7f7", "tertiaryColor": "#ffffff"}}}%%
classDiagram
    direction LR

    class User {
        +Long userId
        +String username
        +UserRole role
    }

    class Quest {
        +Long questId
        +String title
        +QuestStatus status
        +Integer rewardXp
    }

    class PullRequest {
        +Long pullRequestId
        +String externalPrId
        +String status
        +belongsTo(repository) Boolean
    }

    class Submission {
        +Long submissionId
        +Long questId
        +Long submitterId
        +Long pullRequestId
        +String status
        +isLinkedToQuest(quest) Boolean
        +markInvalid(reason) void
    }

    class ReviewRecord {
        +Long reviewId
        +Long submissionId
        +Long reviewerId
        +String decision
        +String summary
        +requiresChanges() Boolean
    }

    class ReviewItem {
        +Long itemId
        +Long reviewId
        +String checkpoint
        +String comment
        +Boolean passed
        +markPassed() void
    }

    class AdminReviewRecord {
        +Long adminReviewId
        +Long questId
        +Long adminId
        +String decision
        +String reason
        +approvePublish() void
        +rejectPublish(reason) void
    }

    class ReviewService {
        +submitWork(command) Submission
        +reviewSubmission(command) ReviewRecord
        +reviewQuest(command) AdminReviewRecord
    }

    class GrowthProfile {
        +Long profileId
        +Long userId
        +Integer totalXp
        +Integer level
        +addXp(amount) void
        +canLevelUp() Boolean
    }

    class XpTransaction {
        +Long transactionId
        +Long userId
        +Long questId
        +Integer amount
        +String reason
    }

    class ContributionRecord {
        +Long recordId
        +Long userId
        +Long questId
        +Long repositoryId
        +String summary
    }

    class GrowthService {
        +handleReviewApproved(event) void
        +getProfile(userId) GrowthProfile
        +listContributions(userId) List~ContributionRecord~
    }

    Submission "*" --> "1" Quest : 对应任务
    Submission "*" --> "1" User : 由提交人创建
    Submission "0..*" --> "0..1" PullRequest : 关联合并请求
    Submission "1" *-- "0..*" ReviewRecord : 拥有审核历史
    ReviewRecord "1" *-- "0..*" ReviewItem : 包含检查项
    AdminReviewRecord "*" --> "1" Quest : 管理发布审核
    AdminReviewRecord "*" --> "1" User : 由管理员执行
    ReviewService ..> Submission : 接收成果
    ReviewService ..> ReviewRecord : 记录维护者审核
    ReviewService ..> AdminReviewRecord : 记录管理员决策
    GrowthProfile "1" --> "1" User : 属于用户
    XpTransaction "*" --> "1" User : 奖励用户
    XpTransaction "*" --> "1" Quest : 来源任务完成
    ContributionRecord "*" --> "1" User : 汇总贡献
    ContributionRecord "*" --> "1" Quest : 引用已完成任务
    GrowthService ..> GrowthProfile : 更新等级
    GrowthService ..> XpTransaction : 写入 XP 流水
    GrowthService ..> ContributionRecord : 写入贡献历史
```

可视化说明

- 提交记录把完成成果和合并请求证据关联起来。
- 审核记录保存结论，审核项保存细粒度反馈。
- 管理员审核独立处理任务发布和下架治理。
- 审核通过后触发成长流水和贡献记录。

## 5. 推荐与通知

```mermaid
%%{init: {"theme": "base", "themeVariables": {"primaryColor": "#ffffff", "primaryTextColor": "#111111", "primaryBorderColor": "#111111", "lineColor": "#111111", "secondaryColor": "#f7f7f7", "tertiaryColor": "#ffffff"}}}%%
classDiagram
    direction LR

    class User {
        +Long userId
        +String username
        +String role
    }

    class Quest {
        +Long questId
        +String title
        +String difficulty
        +Integer rewardXp
    }

    class RecommendationStrategy {
        <<interface>>
        +score(user, quest) Decimal
        +reason(user, quest) String
    }

    class TechStackMatchStrategy {
        +score(user, quest) Decimal
        +reason(user, quest) String
    }

    class GrowthStageStrategy {
        +score(user, quest) Decimal
        +reason(user, quest) String
    }

    class RecommendationResult {
        +Long resultId
        +Long userId
        +Long questId
        +Decimal score
        +String reasonText
        +isStrongMatch() Boolean
    }

    class RecommendationService {
        +recommendQuests(userId) List~RecommendationResult~
        +recommendContributors(questId) List~User~
        +explain(userId, questId) String
    }

    class Notification {
        +Long notificationId
        +Long receiverId
        +String type
        +String content
        +String status
        +markAsRead() void
    }

    class NotificationPreference {
        +Long preferenceId
        +Long userId
        +Boolean enableEmail
        +Boolean enableDigest
        +update(email, digest) void
    }

    class NotificationSender {
        <<interface>>
        +send(notification) SendResult
        +supports(type) Boolean
    }

    class InAppNotificationSender {
        +send(notification) SendResult
        +supports(type) Boolean
    }

    class EmailNotificationSender {
        +send(notification) SendResult
        +supports(type) Boolean
    }

    class NotificationService {
        +notifyQuestAccepted(assignment) void
        +notifyReviewResult(review) void
        +sendDailyDigest(userId) void
    }

    RecommendationStrategy <|.. TechStackMatchStrategy : 计算技术匹配
    RecommendationStrategy <|.. GrowthStageStrategy : 计算成长匹配
    RecommendationService ..> RecommendationStrategy : 组合推荐策略
    RecommendationService ..> RecommendationResult : 生成可解释结果
    RecommendationResult "*" --> "1" User : 推荐给用户
    RecommendationResult "*" --> "1" Quest : 推荐任务
    Notification "*" --> "1" User : 投递给接收人
    NotificationPreference "1" --> "1" User : 配置通知渠道
    NotificationSender <|.. InAppNotificationSender : 写入站内通知
    NotificationSender <|.. EmailNotificationSender : 发送邮件
    NotificationService ..> NotificationSender : 选择投递渠道
    NotificationService ..> Notification : 创建通知
```

可视化说明

- 推荐策略支持扩展评分规则且不修改服务编排。
- 推荐结果持久化用户与任务的可解释匹配。
- 通知偏好独立配置投递渠道和汇总策略。
- 发送接口隔离具体投递方式和业务流程。

## 6. 设计模式应用说明

| 设计模式        | 应用位置                                                 | 使用理由                                                         | 如果不使用的后果                                                |
| --------------- | -------------------------------------------------------- | ---------------------------------------------------------------- | --------------------------------------------------------------- |
| 适配器模式      | `CodeHostAdapter`、`GitHubImportAdapter`、`GiteaAdapter` | 屏蔽 GitHub 与 Gitea API 差异，避免业务模块直接依赖外部平台      | 任务、审核、新手引导模块会散落外部 API 调用，后续替换底座成本高 |
| 策略模式        | `RecommendationStrategy` 及其实现类                      | 推荐系统是核心能力，后续需要按技术栈、成长阶段、历史行为扩展规则 | 新增推荐规则时会频繁修改 `RecommendationService`，违反开闭原则  |
| 策略 / 端口模式 | `NotificationSender` 及其实现类                          | 站内通知和邮件通知有不同投递方式，但上层通知流程应保持一致       | 通知服务会同时处理业务规则和具体通道，单一职责不清              |
