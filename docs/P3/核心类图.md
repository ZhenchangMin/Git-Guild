# Git Guild 核心类图

## 1. 说明

本文件只展示核心类之间的关系，不在类图中展开全部属性和方法，以保证 GitHub 预览时图形清晰。每个类的属性、方法和标注说明见 `类成员说明.md`。

类图按边界上下文拆分为四组：

| 分组           | 覆盖范围                                   |
| -------------- | ------------------------------------------ |
| 用户与外部支撑 | 用户、认证、外部代码托管适配               |
| 任务核心       | 仓库、Issue、任务、分类、标签、接取        |
| 提交审核与成长 | 成果提交、维护者审核、管理员审核、成长记录 |
| 推荐与通知     | 推荐策略、推荐结果、通知偏好、通知投递     |

## 2. 用户与外部支撑

```mermaid
classDiagram
    direction LR

    class User
    class UserRole {
        <<enumeration>>
        BEGINNER
        MAINTAINER
        ADMIN
    }
    class AuthService
    class CodeHostAccountBinding
    class CodeHostAdapter {
        <<interface>>
    }
    class GitHubImportAdapter
    class GiteaAdapter
    class Repository

    User --> UserRole : 角色
    User "1" o-- "0..*" CodeHostAccountBinding : 绑定外部账号
    AuthService ..> User : 注册/登录

    CodeHostAdapter <|.. GitHubImportAdapter : GitHub 导入
    CodeHostAdapter <|.. GiteaAdapter : Gitea 底座
    GitHubImportAdapter ..> Repository : 导入仓库
    GiteaAdapter ..> Repository : 同步仓库状态
```

## 3. 任务核心

```mermaid
classDiagram
    direction LR

    class Repository
    class Issue
    class PullRequest
    class ProjectGuide
    class Quest
    class QuestStatus {
        <<enumeration>>
        DRAFT
        PENDING_ADMIN_REVIEW
        PUBLISHED
        IN_PROGRESS
        IN_REVIEW
        COMPLETED
    }
    class Difficulty {
        <<enumeration>>
        A
        B
        C
        D
    }
    class QuestCategory
    class QuestTag
    class QuestAssignment
    class QuestFilterCriteria
    class QuestService
    class User

    Repository "1" *-- "0..*" Issue : 包含
    Repository "1" *-- "0..*" PullRequest : 包含
    Repository "1" *-- "0..1" ProjectGuide : 项目指引

    Quest --> QuestStatus : 状态
    Quest --> Difficulty : 难度
    Quest "*" --> "1" Repository : 来源仓库
    Quest "0..*" --> "1" Issue : 来源 Issue
    Quest "*" --> "1" User : 发布者
    Quest "*" --> "1" QuestCategory : 分类
    Quest "*" o-- "0..*" QuestTag : 标签
    Quest "1" *-- "0..*" QuestAssignment : 接取记录
    QuestAssignment "*" --> "1" User : 接取人

    QuestService ..> Quest : 创建/发布/重开
    QuestService ..> QuestAssignment : 接取任务
    QuestService ..> QuestFilterCriteria : 筛选任务
```

## 4. 提交审核与成长

```mermaid
classDiagram
    direction LR

    class User
    class Quest
    class PullRequest
    class Submission
    class ReviewRecord
    class ReviewItem
    class AdminReviewRecord
    class ReviewService
    class GrowthProfile
    class XpTransaction
    class ContributionRecord
    class GrowthService

    Submission "*" --> "1" Quest : 对应任务
    Submission "*" --> "1" User : 提交人
    Submission "0..*" --> "0..1" PullRequest : 关联 PR
    Submission "1" *-- "0..*" ReviewRecord : 审核记录
    ReviewRecord "1" *-- "0..*" ReviewItem : 逐项意见
    AdminReviewRecord "*" --> "1" Quest : 任务发布审核
    AdminReviewRecord "*" --> "1" User : 管理员

    ReviewService ..> Submission : 提交成果
    ReviewService ..> ReviewRecord : 维护者审核
    ReviewService ..> AdminReviewRecord : 管理员审核

    GrowthProfile "1" --> "1" User : 成长档案
    XpTransaction "*" --> "1" User : XP 归属
    XpTransaction "*" --> "1" Quest : XP 来源
    ContributionRecord "*" --> "1" User : 贡献者
    ContributionRecord "*" --> "1" Quest : 完成任务
    GrowthService ..> GrowthProfile : 更新等级
    GrowthService ..> XpTransaction : 记录 XP
    GrowthService ..> ContributionRecord : 记录贡献
```

## 5. 推荐与通知

```mermaid
classDiagram
    direction LR

    class User
    class Quest
    class RecommendationStrategy {
        <<interface>>
    }
    class TechStackMatchStrategy
    class GrowthStageStrategy
    class RecommendationResult
    class RecommendationService
    class Notification
    class NotificationPreference
    class NotificationSender {
        <<interface>>
    }
    class InAppNotificationSender
    class EmailNotificationSender
    class NotificationService

    RecommendationStrategy <|.. TechStackMatchStrategy : 技术栈匹配
    RecommendationStrategy <|.. GrowthStageStrategy : 成长阶段匹配
    RecommendationService ..> RecommendationStrategy : 组合策略
    RecommendationService ..> RecommendationResult : 生成结果
    RecommendationResult "*" --> "1" User : 被推荐用户
    RecommendationResult "*" --> "1" Quest : 被推荐任务

    Notification "*" --> "1" User : 接收人
    NotificationPreference "1" --> "1" User : 用户偏好
    NotificationSender <|.. InAppNotificationSender : 站内通知
    NotificationSender <|.. EmailNotificationSender : 邮件通知
    NotificationService ..> NotificationSender : 选择投递通道
    NotificationService ..> Notification : 创建通知
```

## 6. 设计模式应用说明

| 设计模式        | 应用位置                                                 | 使用理由                                                         | 如果不使用的后果                                                |
| --------------- | -------------------------------------------------------- | ---------------------------------------------------------------- | --------------------------------------------------------------- |
| 适配器模式      | `CodeHostAdapter`、`GitHubImportAdapter`、`GiteaAdapter` | 屏蔽 GitHub 与 Gitea API 差异，避免业务模块直接依赖外部平台      | 任务、审核、新手引导模块会散落外部 API 调用，后续替换底座成本高 |
| 策略模式        | `RecommendationStrategy` 及其实现类                      | 推荐系统是核心能力，后续需要按技术栈、成长阶段、历史行为扩展规则 | 新增推荐规则时会频繁修改 `RecommendationService`，违反开闭原则  |
| 策略 / 端口模式 | `NotificationSender` 及其实现类                          | 站内通知和邮件通知有不同投递方式，但上层通知流程应保持一致       | 通知服务会同时处理业务规则和具体通道，单一职责不清              |
