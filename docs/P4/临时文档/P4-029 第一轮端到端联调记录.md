# P4-029 第一轮端到端联调记录

## 1. 联调目标

本记录用于沉淀 P4-029 第一轮端到端联调过程中的环境信息、演示数据、执行结果和 Bug 线索。

核心链路：

登录 -> 发布任务 -> 管理员审核上架 -> 冒险家接取 -> Gitea 操作与 PR -> 提交成果 -> 维护者审核 -> 成长激励更新。

## 2. 本地 Gitea 环境

| 项目 | 内容 |
| --- | --- |
| 用途 | 本机 P4-029 端到端联调 |
| Gitea 地址 | `http://localhost:3000` |
| Docker 服务 | `gitguild-gitea` |
| 数据持久化 | Docker volume `gitea-data` |
| 说明 | 该账号仅用于本地 Gitea，不等同于 Git Guild 系统登录账号。 |

### 2.1 Gitea 初始化管理员账号

| 字段 | 值 |
| --- | --- |
| 管理员用户名 | `admin-N0vemberRain` |
| 电子邮件地址 | `N0vemberRain@gitguild.local` |
| 管理员密码 | `gitguildNovRain` |

### 2.2 Gitea 测试仓库与业务库映射

| 项目                       | 值                                                                 |
| ------------------------ | ----------------------------------------------------------------- |
| 测试仓库 URL                 | `http://localhost:3000/admin-N0vemberRain/gitguild-demo.git`      |
| 测试 Issue 编号              | `#1`                                                              |
| 测试 Issue URL             | `http://localhost:3000/admin-N0vemberRain/gitguild-demo/issues/1` |
| 测试 PR 编号                 | `#2`                                                              |
| 测试 PR URL                | `http://localhost:3000/admin-N0vemberRain/gitguild-demo/pulls/2`  |
| 测试 PR 初始状态               | `OPEN`，暂不合并                                                       |
| Git Guild `repositoryId` | `1`                                                                |
| Git Guild `issueId`      | `1`                                                                |
| Gitea Token 环境变量         | 已生成并在本机配置 `GITEA_TOKEN`，不记录完整 Token                               |

## 3. Git Guild 联调账号

| 角色 | 账号说明 | 业务库记录 |
| --- | --- | --- |
| 委托人 | 已在前端登录并用于发布 P4-029 测试委托 | `user_id=6`，`role=MAINTAINER` |
| 管理员 | 已在前端登录并审核通过 P4-029 测试委托 | `user_id=7`，`username=admin`，`role=ADMIN` |
| 冒险家 | 已在前端登录并接取 P4-029 测试委托 | `user_id=4`，`role=BEGINNER` |

## 4. 主流程执行记录

### 4.1 Gitea 初始化与测试素材准备

| 步骤 | 操作 | 结果 |
| --- | --- | --- |
| Gitea 初始化 | 首次打开 `http://localhost:3000` 完成本地 Gitea 初始化 | 成功 |
| 创建 Gitea 管理员 | 创建 `admin-N0vemberRain` | 成功 |
| 创建测试仓库 | 创建公开仓库 `gitguild-demo` | 成功，URL 为 `http://localhost:3000/admin-N0vemberRain/gitguild-demo.git` |
| 创建测试 Issue | 创建 `P4-029 demo issue` | 成功，Gitea Issue 编号为 `#1` |
| 创建测试 PR | 从 `feature/p4-029-demo` 向 `main` 创建 PR | 成功，Gitea PR 编号为 `#2`，当前保持 `OPEN`，暂不合并 |
| 生成 Gitea Token | 创建 `gitguild-p4-029` Token 并配置到本机后端环境变量 | 成功，不记录完整 Token |

### 4.2 仓库导入与 Issue 同步

| 步骤 | 操作 | 预期结果 | 实际结果 |
| --- | --- | --- | --- |
| 导入仓库 | 委托人在发布委托面板填写 Gitea 仓库 URL，并点击“导入仓库并使用” | 返回业务库 `repositoryId`，仓库状态为 `SYNCED` | 成功，当前使用 `repositoryId=1` |
| 重复导入验证 | 重复点击“导入仓库并使用” | 不再生成新的业务库仓库编号 | 初次联调发现会重复新增，已登记 `BUG-001` 并修复 |
| 读取 Issue | 点击“读取 Issue 列表” | 自动显示 Gitea Issue `#1` 对应的业务库 Issue | 初次联调发现为空，已登记 `BUG-002` 并修复；当前使用 `issueId=1` |

### 4.3 发布委托

| 字段 | 记录 |
| --- | --- |
| 发布账号 | 委托人账号，业务库 `user_id=6`，角色 `MAINTAINER` |
| 仓库 | `repositoryId=1`，`gitguild-demo` |
| Issue | `issueId=1`，Gitea Issue `#1` |
| 委托标题 | `P4-029 demo issue` |
| 难度 | `B` |
| 技术栈 | `Vue,Spring Boot,Gitea` |
| 预计工时 | `2` |
| 奖励 XP | `80` |
| 创建结果 | 成功 |
| Git Guild `questId` | `1` |
| 发布后状态 | `PENDING_ADMIN_REVIEW` |

说明：发布委托阶段发现分类区域中文乱码，已登记 `BUG-003` 并修复。

### 4.4 管理员审核上架

| 字段 | 记录 |
| --- | --- |
| 审核账号 | 管理员账号，业务库 `user_id=7`，`username=admin` |
| 审核接口 | `POST /api/v1/quests/1/admin-reviews` |
| 审核决策 | `APPROVE_PUBLISH` |
| 审核记录 ID | `admin_review_id=1` |
| 审核前状态 | `PENDING_ADMIN_REVIEW` |
| 审核后状态 | `PUBLISHED` |
| 审核时间 | `2026-06-04 02:38:28` |
| 联调结果 | 成功，测试委托已上架 |

### 4.5 冒险家查看与接取委托

| 字段 | 记录 |
| --- | --- |
| 冒险家账号 | 业务库 `user_id=4`，角色 `BEGINNER` |
| 可见性检查 | 冒险家登录后可查看测试委托 |
| 接取操作 | 在前端点击接取委托 |
| Git Guild `assignmentId` | `1` |
| 接取前任务状态 | `PUBLISHED` |
| 接取后任务状态 | `IN_PROGRESS` |
| 接取记录状态 | `ACTIVE` |
| 接取时间 | `2026-06-04 03:21:49` |
| 联调结果 | 成功 |

说明：接取成功后前端不再展示“接取委托”按钮，因此本轮没有通过页面重复点击验证重复接取异常。后续 P4-031 可通过接口或集成测试覆盖重复接取场景。

### 4.6 当前进度

当前 P4-029 主链路已完成：

1. 本地 Gitea 初始化。
2. Gitea 测试仓库、Issue、PR 准备。
3. 委托人导入仓库并读取 Issue。
4. 委托人发布测试委托。
5. 管理员审核通过并上架。
6. 冒险家查看并接取委托。

下一步：

1. 进入提交柜台或提交草稿入口。
2. 关联 Gitea PR `#2`，确保业务库生成对应 `pullRequestId`。
3. 冒险家提交成果，生成 `submissionId`。
4. 在 PR 未合并时尝试审核通过，验证被 `PR_NOT_MERGED` 拦截。
5. 合并 Gitea PR 后重新同步并完成成果审核。
6. 验证任务完成、XP 和成长记录更新。

## 5. Bug 线索

发现问题后同步登记到 `docs/P4/Bug修复日志.md`。

1. 发布委托时重复点击“导入仓库并使用”会增加业务库仓库编号 -- 已修复，见 `BUG-001`
2. 同步 Issue 无响应 / Issue 列表为空 -- 已修复，见 `BUG-002`
3. 发布委托时“分类”字段中文乱码 -- 已修复，见 `BUG-003`
4. 管理员任务审核前端背景有问题，背景重复？粘连组件？ -- ***待修复***
5. 管理员控制台-平台配置-难度选项，A~D级顺序反了 -- ***待调整***
6. 冒险家查看委托时，委托清单与委托详情上的难度等级、XP、标签、技术栈不一致，初步判断委托详情是mock数据或者接口连接错误 -- ***已检查修复***
7. 工作台前端仍然是mock数据 -- ***待修改***
8. 