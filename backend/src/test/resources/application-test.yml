spring:
  datasource:
    url: jdbc:h2:mem:gitguild-test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  data:
    redis:
      repositories:
        enabled: false
  # 测试无需真实发信，仅为让 JavaMailSender Bean 可被注入（MailSenderAutoConfiguration 依赖 host）。
  mail:
    host: localhost
    port: 3025

jwt:
  secret: test-secret-for-gitguild-auth-module-32bytes
  expiration-ms: 3600000
  refresh-expiration-ms: 86400000
